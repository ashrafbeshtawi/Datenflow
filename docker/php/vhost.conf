<VirtualHost *:80>
    DocumentRoot /app/public

    <Directory /app/public>
        AllowOverride None
        Require all granted
        # Route everything that is not an existing file to Symfony's front controller
        FallbackResource /index.php
    </Directory>
</VirtualHost>
