framework:
    rate_limiter:
        form_submit:
            policy: sliding_window
            limit: 5
            interval: '15 minutes'

when@test:
    framework:
        rate_limiter:
            form_submit:
                policy: sliding_window
                limit: 1000
                interval: '15 minutes'
