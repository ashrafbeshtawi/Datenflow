<section class="cta-band">
    <div class="wrap cta-band-inner">
        <div>
            <h2>{{ t.cta_band.title }}</h2>
            <p>{{ t.cta_band.body }}</p>
        </div>
        <a class="btn btn-primary btn-lg" href="{{ path('booking') }}">{{ t.cta_band.button }}</a>
    </div>
</section>
